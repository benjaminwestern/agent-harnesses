Use the `bash` tool to create `.artifacts/opencode-hook-approval.txt` with the
text `opencode approval probe`. Do not ask for an alternative approach. Wait
for the permission prompt, proceed after approval, and then confirm the final
path you wrote.
