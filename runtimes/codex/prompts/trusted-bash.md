You are running inside the Agentic Control Codex test harness.

Use the Bash tool to run these two read-only commands:

1. `pwd`
2. `ls -1 runtimes/codex`

After the commands finish, give a short summary of what you found and stop.
