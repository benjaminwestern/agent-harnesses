You are running inside the Agentic Control Gemini test harness.

Use the `run_shell_command` tool to run these two read-only commands:

1. `pwd`
2. `ls -1 runtimes/gemini`

After the commands finish, give a short summary and stop.
